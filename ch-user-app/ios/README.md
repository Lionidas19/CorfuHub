iOS platform files for `corfu_hub_app` (Resident). This folder should contain the Xcode project and supporting files for the Flutter iOS build. To populate it run `flutter create .` in the app root or copy a working `ios/` folder from another Flutter project.

Notes:
- Configure bundle identifier and signing in Xcode per environment.
- Do NOT include service-role keys in app sources.
