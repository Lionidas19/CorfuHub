Android platform files for `corfu_hub_app` (Resident). This folder contains Android project artifacts for the Flutter app. To populate an Android platform for a Flutter app run `flutter create .` in the app root or copy a working `android/` folder from another Flutter project.

Notes:
- The app package id and launcher icons should be set per-environment and per-app.
- Do NOT include service-role keys in app sources.
