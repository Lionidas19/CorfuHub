name: corfu_hub_admin_app
description: CorfuHub Admin app — moderation, dispute handling, enforcement and operations UI for the Corfu Map Platform.
publish_to: "none"
version: 0.1.0
environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=2.5.0"
dependencies:
  flutter:
    sdk: flutter
  corfu_shared:
    path: ../shared

flutter:
  uses-material-design: true
