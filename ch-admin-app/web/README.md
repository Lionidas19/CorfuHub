Web platform files for `corfu_hub_admin_app` (Admin). This folder contains the web entrypoint and static assets for the Admin Flutter web build.

Notes:
- The `index.html` should reference the compiled `main.dart.js` produced by `flutter build web`.
- Do NOT include service-role keys in client-side sources.
