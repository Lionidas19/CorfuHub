Android platform files go here. For a full Flutter app, run `flutter create .` in this folder or copy the `android/` folder from an existing Flutter app.
Android platform files for `corfu_hub_owner_app` (Owner). This folder contains Android project artifacts for the Owner Flutter app. To populate an Android platform for a Flutter app run `flutter create .` in the app root or copy a working `android/` folder from another Flutter project.

Notes:
- Package id and launcher icons should be configured per-app and per-environment.
- Do NOT include service-role keys in app sources.