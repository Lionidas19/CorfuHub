Web platform files for `corfu_hub_owner_app` (Owner). This folder contains the web entrypoint and static assets for the Owner Flutter web build.

Notes:
- The `index.html` should reference the compiled `main.dart.js` produced by `flutter build web`.
- Do NOT include service-role keys in client-side sources.
